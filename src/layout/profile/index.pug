extends ../template/index
include ../../components/profileField/profileField

block content
    .sidebar
        img.sidebar__navigate-back(src="/static/images/back.svg" data-ref="navigate")
    .profile
        .profile__avatar
            img(src="/static/images/defaultAvatar.svg")
        h1.profile__title Иван
        .profile__fields(data-ref="profileFields")
            block profile-fields
        block profile-actions
